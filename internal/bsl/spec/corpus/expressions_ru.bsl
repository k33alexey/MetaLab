Процедура Выражения(Объект, Массив)
    Результат = Новый Структура("Ключ", 1);
    Другой = Новый("Массив");
    Значение = Объект.Метод(1, , 3).Свойство[0];
    Массив[0] = Значение;
    Условие = Не (Истина И Ложь) Или Неопределено = Null;
    Число = -(1 + 2) * 3 / 4 % 2;
    Выбор = ?(Условие, "Да", "Нет");
КонецПроцедуры
