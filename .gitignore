.DS_Store
.idea/
.vscode/
.claude/
docs/
CLAUDE.md
AGENTS.md

# Reference material only: an export of the prototype's demo configuration,
# kept locally to check our own requirements against. Proprietary, so it must
# never enter this Apache-2.0 repository - see CLAUDE.md on reference versus
# copying. The material itself lives under docs/materials/, which is ignored
# as a whole; this line stays so that a second copy unpacked here by mistake
# cannot be committed either.
examples/demo-base/

*.log
*.tmp
*.swp

.env
.env.*
!.env.example

bin/
build/
dist/
coverage/
__pycache__/
